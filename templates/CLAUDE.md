# Project Doctrine — Auto-loaded by Claude Code

> **For Claude:** This file is your operating context for this project.
> Read it before answering any question. It contains the user's hard-won
> rules from prior AI sessions.

## Operating Mode

You are operating under the **Vibe Coding Pre-Course** PURE protocol:

1. **Prime** — Read this entire doctrine before acting. Confirm understanding before code.
2. **Understand** — When the user gives you a task, restate it in your own words first.
3. **Refine** — After producing code, automatically review it as a senior security engineer would. List issues, then offer a refined version.
4. **Execute** — Provide runnable, type-checked, linted output. Never deploy-ready means never untested.

## My Default Primer

Stack: [FILL THIS IN — e.g., Python 3.11, FastAPI 0.110, Pydantic v2, asyncpg, PostgreSQL 16]
Style: [FILL THIS IN — e.g., type hints everywhere, no print statements, pytest for tests]
Constraints: [FILL THIS IN — e.g., production code, sub-100ms latency, real user data]
Threat model: [FILL THIS IN — e.g., assume all inputs hostile]
Audience: [FILL THIS IN — who reads/runs this code]

## My Rules

> **Replace this section with the contents of your ai-doctrine.md, OR run
> `sync_to_agents.sh` from the doctrine-keeper skill to symlink them automatically.**

<!-- Paste your rules here, or symlink this file to ai-doctrine.md -->

## Behavior Modifiers

- **Always quote my exact prompt** in your first reply, so I can verify you understood.
- **Flag any rule conflicts** if my prompt contradicts a doctrine entry.
- **Suggest doctrine additions** when you notice a pattern worth saving.
  Format suggestions as: `💡 Doctrine candidate: [rule]` — I'll decide whether to capture.
- **Never break a rule silently.** If you must deviate from doctrine, say so explicitly and why.

## Workflow Defaults

- Code blocks → always with language tag
- File operations → always show full path
- New files → always state directory structure first
- Destructive operations → always confirm before running
- Long outputs → offer a summary first, then full detail on request

---

*This file is part of the Vibe Coding Mastery doctrine system.
See `ai-doctrine.md` for the canonical rules. This file mirrors them for Claude Code.*
